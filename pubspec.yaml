name: arrow_path
description: Draw arrows with Path objects easily. Paths can be composited to add arrows to any curve and draw all at once.
version: 3.1.0
homepage: https://github.com/quentinleguennec/flutter-arrow-path.git

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.1
