name: arrow_path_example
description: Example project for the arrow_path package.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  arrow_path:
      path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
